# Server Configuration
server.port=8080
server.servlet.context-path=/

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://admin:Password1455@skillforge.gdfj2so.mongodb.net/?retryWrites=true&w=majority&appName=SkillForge
spring.data.mongodb.database=skillforge

# CORS Configuration
management.endpoints.web.cors.allow-credentials=true
management.endpoints.web.cors.allowed-origins=http://localhost:4200
management.endpoints.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
management.endpoints.web.cors.allowed-headers=*

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# Logging
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.com.skillforge=DEBUG

# Application Properties
app.name=SkillForge
app.version=1.0.0
app.description=E-Learning Platform

# JWT Configuration (if implementing JWT later)
jwt.secret=mySecretKey
jwt.expiration=86400000